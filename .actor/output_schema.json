{
  "actorOutputSchemaVersion": 1,
  "title": "AI Job Finder Output",
  "description": "Links to actor outputs stored in dataset and key-value store.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "Job results",
      "description": "All output records from the default dataset.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "datasetOverview": {
      "type": "string",
      "title": "Dataset overview view",
      "description": "Dataset output with the overview view selected by default.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?view=overview"
    },
    "runSummary": {
      "type": "string",
      "title": "Run summary records",
      "description": "Dataset records that contain run-level diagnostics and counts.",
      "template": "{{links.apiDefaultDatasetUrl}}/items?clean=true"
    },
    "files": {
      "type": "string",
      "title": "Key-value store records",
      "description": "All records in the default key-value store.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/keys"
    }
  }
}
